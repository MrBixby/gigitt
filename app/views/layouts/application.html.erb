<!DOCTYPE html>
<html>
<head>
<%= favicon_link_tag '/img/favicon-128.ico' %>
  <title>championing dreams, one gig at a time, gigitt.com</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <meta name="keywords" content="Gigitt, music, bands, venues, promoters, book shows, book gigs" />
  <meta name="description" content="Gigitt.com The crowd pleaser! A place for fans venues and musicians. Coming together to create a perfect event. Let your voice be heard." />
</head>
<body>
  <nav class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <div class="navbar-header">
          <a href="/" class="pull-left"><img style="height: 50px; padding-top: 5px;" src="/img/logoMonster.png" alt="Gigitt Logo"></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><%= link_to "Home", root_path %></li>
              <li><%= link_to "Dashboard", dashboard_path %></li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= current_user.username %><span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to "Edit User", edit_path %></li>
                  <li><strong><%= link_to "Public Profile", current_user.profile %></strong></li>
                  <li><%= link_to "Edit Profile", edit_profile_path(current_user.profile) %></li>
                  <li><%= link_to "Switch User" %></li>
                </ul>
              <li><%= link_to "Log Out", logout_path %></li>
            <% else %>
              <li><%= link_to "Register", register_path %></li>
              <li><%= link_to "Log In", login_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </nav>
    <div class="container">
      <div class="content">
        <% flash.each do |type, message| %>

        <% end %>
        <% if flash[:notice] %>
          <div class="alert alert-success">
            <button class="close" data-dismiss="alert">x</button>
            <%= flash[:notice] %>
          </div>
        <% end %>

        <% if flash[:alert] %>
          <div class="alert alert-error">
            <button class="close" data-dismiss="alert">x</button>
            <%= flash[:alert] %>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
</body>
</html>
