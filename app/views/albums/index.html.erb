<h1>Albums</h1>
<% @albums.each do |album| %>
<div class="panel panel-default">
  <ul class="list-group">
    <li class="list-group-item"><%= link_to(album.title, album_path(album)) %></li>
  </ul>
</div>
<% end %>
